services:
  - type: web
    name: eeg-motor-imagery-backend
    runtime: python
    rootDir: backend
    buildCommand: git lfs pull && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
